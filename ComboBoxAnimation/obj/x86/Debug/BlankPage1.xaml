<Page xmlns:control="using:ComboBoxAnimation.Controls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ComboBoxAnimation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:ComboBoxAnimation.ViewModel" 
    x:Class="ComboBoxAnimation.BlankPage1"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.DataContext>
            <ViewModel:ModelView/>
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Popup x:ConnectionId='1' Name="Popup" 
               CompositeMode="Inherit"
                               IsOpen="True"
               HorizontalAlignment="Stretch"
                               Grid.Row="0">
            <Grid HorizontalAlignment="Stretch">
                <StackPanel x:ConnectionId='2' Name="PopupPanel"
                                        Background="Transparent"                                         
                                        Orientation="Vertical"
                                        HorizontalAlignment="Stretch" >
                    <StackPanel.Projection>
                        <PlaneProjection LocalOffsetY="0"/>
                    </StackPanel.Projection>
                    <ListBox x:ConnectionId='3' Name="listBox"             
                                         HorizontalAlignment="Stretch"
                                         ItemsSource="{Binding Users}"
                                         HorizontalContentAlignment="Stretch"                             
                                         Background="#CCFFFFFF"
                                         BorderBrush="Black"
                                         BorderThickness="1">
                        <ListBox.Projection>
                            <PlaneProjection LocalOffsetY="0"/>
                        </ListBox.Projection>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Grid>
        </Popup>
    </Grid>
</Page>

